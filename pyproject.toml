[project]
name = "steam-artwork-uploader"
version = "0.1.0"
description = "CLI tool for uploading images to Steam Community Artwork"
requires-python = ">=3.10"
license = "MIT"
dependencies = [
    "curl_cffi",
    "rich",
]

[project.scripts]
steam_artwork_uploader = "steam_artwork_uploader:main"
